import React from 'react';
import { Text, AppRegistry } from 'react-native';

const App = () => (
    <Text>This is MusicCollectionApp</Text>
);

AppRegistry.registerComponent('MusicCollectionApp', () => App);